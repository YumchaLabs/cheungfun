{
  "model": "${EMBEDDING_MODEL:sentence-transformers/all-MiniLM-L6-v2}",
  "dimension": 384,
  "batch_size": 32,
  "max_retries": 3,
  "timeout_seconds": 30,
  "provider": "fastembed",
  "device": "cpu",
  "cache_dir": "./models"
}
